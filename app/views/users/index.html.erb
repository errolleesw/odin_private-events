<h1>Users</h1>
<% if @users.nil? %>
  <p>No users found.</p>
<% else %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.username, user %>
      </li>
    <% end %>
  </ul>
<% end %>
<%= link_to 'New user', new_user_path %>
